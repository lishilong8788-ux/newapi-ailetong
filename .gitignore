.idea
.vscode
.zed
.history
upload
*.exe
# 编辑器/构建留下的备份产物，例如 new-api.route.exe~ —— 末尾的 ~ 让它匹配不上 *.exe，
# 曾经差一步被 git add -A 当成 144MB 的源码提进仓库。
*.exe~
*.db
*.db.bak-*
build
*.db-journal
logs
web/dist
web/node_modules
# Vitest writes its cache to a bare node_modules/.vite at the repo root.
/node_modules
.env
one-api
new-api
/__debug_bin*
.DS_Store
tiktoken_cache
.eslintcache
.gocache
.gomodcache/
.cache
plans
# 排除 .claude 的内容而不是 .claude 本身：目录一旦被排除，git 就不再下降进去，
# 里面的 !example 例外会全部失效。用 .claude/* 才能给 skills 开口子。
.claude/*
!.claude/skills/
.cursor

electron/node_modules
electron/dist
/data/
.gomodcache/
.gocache-temp
.gopath
.test
token_estimator_test.go
skills-lock.json
.playwright-mcp

# Local-only live probes and scratch test workspaces.
.local-tests/
service/relayconvert/chat_responses_live_local_test.go
service/openaicompat/chat_responses_live_local_test.go
go.work
go.work.sum
